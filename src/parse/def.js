/**
 * 定义了一个转换对照表
 */

define( function () {

    var a = true;

    return {

        "combination": a,
        "radical": a,
        "fraction": a,
        "summation": a,
        "integration": a

    };

} );